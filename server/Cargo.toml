[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
birdgen = { path = "../birdgen" }
tokio = { version = "1", features = ["full"] }
warp = { version = "0.4", features = ["server"] }
